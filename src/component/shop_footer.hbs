<section class="col-12 mt-0">
    <div class="row ">
        {{#each footer}}
        <div class="nav flex-column col-2">
            {{#each this}}
            <div class="nav-item">
                <a href={{link}} class="nav-link px-0 {{#if @last}} disabled {{/if}}">{{text}}</a>
            </div>
            {{/each}}
        </div>
        {{/each}}
    </div>
</section>