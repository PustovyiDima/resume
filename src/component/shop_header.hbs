{{#with navigation}}
<div class="card mb-4">
    <section class=" nav ">
        {{#each links}}
        <div class="nav-item ">
            <a href={{href}} class="nav-link  {{#if @last}} disabled {{/if}}"> {{text}} </a>
        </div>
        {{/each}}
    </section>
</div>
{{/with}}